# script pour creer tous les fichiers de commandes irc

touch Join.cpp
touch Join.hpp
touch PrivateMessage.cpp
touch PrivateMessage.hpp
touch Nick.cpp
touch Nick.hpp
touch Part.hpp
touch Part.cpp
touch Quit.cpp
touch Quit.hpp


# if (line.find("NICK") == 0) {
    #     return kNick;
    # }
    # if (line.find("USER") == 0) {
    #     return kUser;
    # }
    # if (line.find("JOIN") == 0) {
    #     return kJoin;
    # }
    # if (line.find("PART") == 0) {
    #     return kPart;
    # }
    # if (line.find("PRIVMSG") == 0) {
    #     return kPrivateMessage;
    # }
    # if (line.find("QUIT") == 0) {
    #     return kQuit;